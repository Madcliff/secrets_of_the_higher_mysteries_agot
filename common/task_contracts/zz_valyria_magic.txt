zz_valyria_magic_visit_library = {
	group = zz_valyria_magic_contracts
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00010 }
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
		}
	}

	# Rewards
	task_contract_reward = {
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					zz_always_random_gain_magic_scroll = yes
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					zz_always_random_gain_magic_scroll = yes
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}


zz_valyria_magic_visit_library_dragonstone = {
	group = zz_valyria_magic_contracts_special
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_location.county.holder = {save_scope_as = employer}
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00010 }
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
		}
	}

	# Rewards
	task_contract_reward = {
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				switch = { 
					trigger = task_contract_location
					province:4415 = { task_contract_taker = { trigger_event = zz_valyria_magic.0011 } } #Dragonstone
					province:3499 = { task_contract_taker = { trigger_event = zz_valyria_magic.0010 } } #Citadel
					province:77 = { task_contract_taker = { trigger_event = zz_valyria_magic.0012 } } #Winterfall
					province:5 = { task_contract_taker = { trigger_event = zz_valyria_magic.0013 } } #Castle Black
					province:1934 = { task_contract_taker = { trigger_event = zz_valyria_magic.0014 } } #Harrenhal
					province:4939 = { task_contract_taker = { trigger_event = zz_valyria_magic.0015 } } #Water Gardens
					province:7708 = { task_contract_taker = { trigger_event = zz_valyria_magic.0016 } } #Pentos
					province:7713 = { task_contract_taker = { trigger_event = zz_valyria_magic.0017 } } #Temple of Rhllor
					province:7559 = { task_contract_taker = { trigger_event = zz_valyria_magic.0018 } } #Myr
					province:7351 = { task_contract_taker = { trigger_event = zz_valyria_magic.0019 } } #Zobti Bastion
					province:7389 = { task_contract_taker = { trigger_event = zz_valyria_magic.0020 } } #Temple of Trade
					province:7388 = { task_contract_taker = { trigger_event = zz_valyria_magic.0021 } } #Lys
					province:8062 = { task_contract_taker = { trigger_event = zz_valyria_magic.0022 } } #The Sealord's Palace
					province:8063 = { task_contract_taker = { trigger_event = zz_valyria_magic.0023 } } #The Arsenal
					province:8064 = { task_contract_taker = { trigger_event = zz_valyria_magic.0024 } } #The Purple Habour
					province:8065 = { task_contract_taker = { trigger_event = zz_valyria_magic.0025 } } #Court of Smiles
					province:8066 = { task_contract_taker = { trigger_event = zz_valyria_magic.0026 } } #Isle of the Gods
					fallback = {
						task_contract_taker = {
							send_interface_toast = {
								title = poi_magic.visit
								left_icon = task_contract_taker
								custom_tooltip = poi_valyria_magic.effect
								if = { 
									limit = { 
										NOT = {
											has_trait = zz_magister
										}
									}
									trigger_event =  zz_valyria_magic.9000
									
								}
								else = {
									random = {
										chance = 75
										trigger_event =  zz_valyria_magic.9000
									}
								}
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				switch = { 
					trigger = task_contract_location
					province:4415 = { task_contract_taker = { trigger_event = zz_valyria_magic.0011 } } #Dragonstone
					province:3499 = { task_contract_taker = { trigger_event = zz_valyria_magic.0010 } } #Citadel
					province:77 = { task_contract_taker = { trigger_event = zz_valyria_magic.0012 } } #Winterfall
					province:5 = { task_contract_taker = { trigger_event = zz_valyria_magic.0013 } } #Castle Black
					province:1934 = { task_contract_taker = { trigger_event = zz_valyria_magic.0014 } } #Harrenhal
					province:4939 = { task_contract_taker = { trigger_event = zz_valyria_magic.0015 } } #Water Gardens
					province:7708 = { task_contract_taker = { trigger_event = zz_valyria_magic.0016 } } #Pentos
					province:7713 = { task_contract_taker = { trigger_event = zz_valyria_magic.0017 } } #Temple of Rhllor
					province:7559 = { task_contract_taker = { trigger_event = zz_valyria_magic.0018 } } #Myr
					province:7351 = { task_contract_taker = { trigger_event = zz_valyria_magic.0019 } } #Zobti Bastion
					province:7389 = { task_contract_taker = { trigger_event = zz_valyria_magic.0020 } } #Temple of Trade
					province:7388 = { task_contract_taker = { trigger_event = zz_valyria_magic.0021 } } #Lys
					province:8062 = { task_contract_taker = { trigger_event = zz_valyria_magic.0022 } } #The Sealord's Palace
					province:8063 = { task_contract_taker = { trigger_event = zz_valyria_magic.0023 } } #The Arsenal
					province:8064 = { task_contract_taker = { trigger_event = zz_valyria_magic.0024 } } #The Purple Habour
					province:8065 = { task_contract_taker = { trigger_event = zz_valyria_magic.0025 } } #Court of Smiles
					province:8066 = { task_contract_taker = { trigger_event = zz_valyria_magic.0026 } } #Isle of the Gods
					fallback = {
						task_contract_taker = {
							send_interface_toast = {
								title = poi_magic.visit
								left_icon = task_contract_taker
								custom_tooltip = poi_valyria_magic.effect
								if = { 
									limit = { 
										NOT = {
											has_trait = zz_magister
										}
									}
									trigger_event =  zz_valyria_magic.9000
									
								}
								else = {
									random = {
										chance = 75
										trigger_event =  zz_valyria_magic.9000
									}
								}
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

# Pratical Magic Experiments 

zz_valyria_magic_practical_magic_experiments_higher_mysteries = {
	group = zz_valyria_magic_contracts_xp
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00100 }
		increment_variable_effect = {
			VAR = magic_experiments_left
			VAL = 3
		}
		### Victory tiers.
		increment_variable_effect = {
			VAR = win_t1_threshold
			VAL = 1 #You smooch by on the standard answer
		}
		increment_variable_effect = {
			VAR = win_t2_threshold
			VAL = 2 #You use your language/traits twice and also have a successful embellishment
		}
		increment_variable_effect = {
			VAR = win_t3_threshold
			VAL = 3 #All three embellishments are successful :yeslikethis:
		}
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
			
		}
	}

	# Rewards
	task_contract_reward = {
		success_qualified = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = secrets_of_the_higher_mysteries
						value = {
							integer_range = {
								min = 3
								max = 5
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = secrets_of_the_higher_mysteries
						value = {
							integer_range = {
								min = 6
								max = 10
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = secrets_of_the_higher_mysteries
						value = {
							integer_range = {
								min = 9
								max = 15
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

zz_valyria_magic_practical_magic_experiments_blood_magic = {
	group = zz_valyria_magic_contracts_xp
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00100 }
		increment_variable_effect = {
			VAR = magic_experiments_left
			VAL = 3
		}
		### Victory tiers.
		increment_variable_effect = {
			VAR = win_t1_threshold
			VAL = 1 #You smooch by on the standard answer
		}
		increment_variable_effect = {
			VAR = win_t2_threshold
			VAL = 2 #You use your language/traits twice and also have a successful embellishment
		}
		increment_variable_effect = {
			VAR = win_t3_threshold
			VAL = 3 #All three embellishments are successful :yeslikethis:
		}
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
			
		}
	}

	# Rewards
	task_contract_reward = {
		success_qualified = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = blood_magic
						value = {
							integer_range = {
								min = 3
								max = 5
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = blood_magic
						value = {
							integer_range = {
								min = 6
								max = 10
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = blood_magic
						value = {
							integer_range = {
								min = 9
								max = 15
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

zz_valyria_magic_practical_magic_experiments_dream_magic = {
	group = zz_valyria_magic_contracts_xp
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00100 }
		increment_variable_effect = {
			VAR = magic_experiments_left
			VAL = 3
		}
		### Victory tiers.
		increment_variable_effect = {
			VAR = win_t1_threshold
			VAL = 1 #You smooch by on the standard answer
		}
		increment_variable_effect = {
			VAR = win_t2_threshold
			VAL = 2 #You use your language/traits twice and also have a successful embellishment
		}
		increment_variable_effect = {
			VAR = win_t3_threshold
			VAL = 3 #All three embellishments are successful :yeslikethis:
		}
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
			
		}
	}

	# Rewards
	task_contract_reward = {
		success_qualified = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = dream_magic
						value = {
							integer_range = {
								min = 3
								max = 5
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = dream_magic
						value = {
							integer_range = {
								min = 6
								max = 10
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = dream_magic
						value = {
							integer_range = {
								min = 9
								max = 15
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

zz_valyria_magic_practical_magic_experiments_alchemy_magic = {
	group = zz_valyria_magic_contracts_xp
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00100 }
		increment_variable_effect = {
			VAR = magic_experiments_left
			VAL = 3
		}
		### Victory tiers.
		increment_variable_effect = {
			VAR = win_t1_threshold
			VAL = 1 #You smooch by on the standard answer
		}
		increment_variable_effect = {
			VAR = win_t2_threshold
			VAL = 2 #You use your language/traits twice and also have a successful embellishment
		}
		increment_variable_effect = {
			VAR = win_t3_threshold
			VAL = 3 #All three embellishments are successful :yeslikethis:
		}
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
			
		}
	}

	# Rewards
	task_contract_reward = {
		success_qualified = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = alchemy_magic
						value = {
							integer_range = {
								min = 3
								max = 5
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = alchemy_magic
						value = {
							integer_range = {
								min = 6
								max = 10
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = alchemy_magic
						value = {
							integer_range = {
								min = 9
								max = 15
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

zz_valyria_magic_practical_magic_experiments_shadow_magic = {
	group = zz_valyria_magic_contracts_xp
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00100 }
		increment_variable_effect = {
			VAR = magic_experiments_left
			VAL = 3
		}
		### Victory tiers.
		increment_variable_effect = {
			VAR = win_t1_threshold
			VAL = 1 #You smooch by on the standard answer
		}
		increment_variable_effect = {
			VAR = win_t2_threshold
			VAL = 2 #You use your language/traits twice and also have a successful embellishment
		}
		increment_variable_effect = {
			VAR = win_t3_threshold
			VAL = 3 #All three embellishments are successful :yeslikethis:
		}
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
			
		}
	}

	# Rewards
	task_contract_reward = {
		success_qualified = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = shadow_magic
						value = {
							integer_range = {
								min = 3
								max = 5
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = shadow_magic
						value = {
							integer_range = {
								min = 6
								max = 10
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = shadow_magic
						value = {
							integer_range = {
								min = 9
								max = 15
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

zz_valyria_magic_practical_magic_experiments_air_magic = {
	group = zz_valyria_magic_contracts_xp
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00100 }
		increment_variable_effect = {
			VAR = magic_experiments_left
			VAL = 3
		}
		### Victory tiers.
		increment_variable_effect = {
			VAR = win_t1_threshold
			VAL = 1 #You smooch by on the standard answer
		}
		increment_variable_effect = {
			VAR = win_t2_threshold
			VAL = 2 #You use your language/traits twice and also have a successful embellishment
		}
		increment_variable_effect = {
			VAR = win_t3_threshold
			VAL = 3 #All three embellishments are successful :yeslikethis:
		}
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
			
		}
	}

	# Rewards
	task_contract_reward = {
		success_qualified = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = air_magic
						value = {
							integer_range = {
								min = 3
								max = 5
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = air_magic
						value = {
							integer_range = {
								min = 6
								max = 10
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = air_magic
						value = {
							integer_range = {
								min = 9
								max = 15
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

zz_valyria_magic_practical_magic_experiments_fire_magic = {
	group = zz_valyria_magic_contracts_xp
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00100 }
		increment_variable_effect = {
			VAR = magic_experiments_left
			VAL = 3
		}
		### Victory tiers.
		increment_variable_effect = {
			VAR = win_t1_threshold
			VAL = 1 #You smooch by on the standard answer
		}
		increment_variable_effect = {
			VAR = win_t2_threshold
			VAL = 2 #You use your language/traits twice and also have a successful embellishment
		}
		increment_variable_effect = {
			VAR = win_t3_threshold
			VAL = 3 #All three embellishments are successful :yeslikethis:
		}
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
			
		}
	}

	# Rewards
	task_contract_reward = {
		success_qualified = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = fire_magic
						value = {
							integer_range = {
								min = 3
								max = 5
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = fire_magic
						value = {
							integer_range = {
								min = 6
								max = 10
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = fire_magic
						value = {
							integer_range = {
								min = 9
								max = 15
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

zz_valyria_magic_practical_magic_experiments_water_magic = {
	group = zz_valyria_magic_contracts_xp
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00100 }
		increment_variable_effect = {
			VAR = magic_experiments_left
			VAL = 3
		}
		### Victory tiers.
		increment_variable_effect = {
			VAR = win_t1_threshold
			VAL = 1 #You smooch by on the standard answer
		}
		increment_variable_effect = {
			VAR = win_t2_threshold
			VAL = 2 #You use your language/traits twice and also have a successful embellishment
		}
		increment_variable_effect = {
			VAR = win_t3_threshold
			VAL = 3 #All three embellishments are successful :yeslikethis:
		}
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
			
		}
	}

	# Rewards
	task_contract_reward = {
		success_qualified = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = water_magic
						value = {
							integer_range = {
								min = 3
								max = 5
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = water_magic
						value = {
							integer_range = {
								min = 6
								max = 10
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = water_magic
						value = {
							integer_range = {
								min = 9
								max = 15
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

zz_valyria_magic_practical_magic_experiments_dragon_magic = {
	group = zz_valyria_magic_contracts_xp
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00100 }
		increment_variable_effect = {
			VAR = magic_experiments_left
			VAL = 3
		}
		### Victory tiers.
		increment_variable_effect = {
			VAR = win_t1_threshold
			VAL = 1 #You smooch by on the standard answer
		}
		increment_variable_effect = {
			VAR = win_t2_threshold
			VAL = 2 #You use your language/traits twice and also have a successful embellishment
		}
		increment_variable_effect = {
			VAR = win_t3_threshold
			VAL = 3 #All three embellishments are successful :yeslikethis:
		}
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
			
		}
	}

	# Rewards
	task_contract_reward = {
		success_qualified = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = dragon_magic
						value = {
							integer_range = {
								min = 3
								max = 5
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = dragon_magic
						value = {
							integer_range = {
								min = 6
								max = 10
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					add_trait_xp = {
						trait = zz_magister
						track = dragon_magic
						value = {
							integer_range = {
								min = 9
								max = 15
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}



zz_valyria_magic_secrets_of_the_dooom = {
	group = zz_valyria_magic_contracts_special
	icon = "gfx/interface/icons/point_of_interest_types/poi_valyria_magic_volcano.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		OR = {
			has_inactive_trait = starwalker
			has_trait = starwalker
		}
	}
	valid_to_accept = {
		# Standard triggers.
		OR = {
			has_inactive_trait = starwalker
			has_trait = starwalker
		}
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = {
			OR = {
				has_inactive_trait = starwalker
				has_trait = starwalker
			}
		}
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = {
			OR = {
				has_inactive_trait = starwalker
				has_trait = starwalker
			}
		}
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_location.county.holder = {save_scope_as = employer}
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00030 }
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
		}
	}

	# Rewards
	task_contract_reward = {
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					send_interface_toast = {
						title = poi_valyria_magic_volcano.visit
						left_icon = root
						custom_tooltip = poi_valyria_magic_volcano.effect
						add_trait_xp = { 
							trait = starwalker 
							track = chosen_of_the_heavens
							value = {
								integer_range = {
									min = 3
									max = 7
								}
							}
						}
						root.location = {
							save_scope_as = task_contract_location
						}
						add_character_flag = dragon_training
						trigger_event = {
							id = zz_valyria_the_doom.001
							days = 1
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				switch = { 
					trigger = task_contract_location
					province:3499 = { task_contract_taker = { trigger_event = zz_valyria_magic.0010 } } #Citadel
					province:4415 = { task_contract_taker = { trigger_event = zz_valyria_magic.0011 } } #Dragonstone
					province:77 = { task_contract_taker = { trigger_event = zz_valyria_magic.0012 } } #Winterfall
					province:5 = { task_contract_taker = { trigger_event = zz_valyria_magic.0013 } } #Castle Black
					province:1934 = { task_contract_taker = { trigger_event = zz_valyria_magic.0014 } } #Harrenhal
					province:4939 = { task_contract_taker = { trigger_event = zz_valyria_magic.0015 } } #Water Gardens
					province:7708 = { task_contract_taker = { trigger_event = zz_valyria_magic.0016 } } #Pentos
					province:7713 = { task_contract_taker = { trigger_event = zz_valyria_magic.0017 } } #Temple of Rhllor
					province:7559 = { task_contract_taker = { trigger_event = zz_valyria_magic.0018 } } #Myr
					province:7351 = { task_contract_taker = { trigger_event = zz_valyria_magic.0019 } } #Zobti Bastion
					province:7389 = { task_contract_taker = { trigger_event = zz_valyria_magic.0020 } } #Temple of Trade
					province:7388 = { task_contract_taker = { trigger_event = zz_valyria_magic.0021 } } #Lys
					province:8062 = { task_contract_taker = { trigger_event = zz_valyria_magic.0022 } } #The Sealord's Palace
					province:8063 = { task_contract_taker = { trigger_event = zz_valyria_magic.0023 } } #The Arsenal
					province:8064 = { task_contract_taker = { trigger_event = zz_valyria_magic.0024 } } #The Purple Habour
					province:8065 = { task_contract_taker = { trigger_event = zz_valyria_magic.0025 } } #Court of Smiles
					province:8066 = { task_contract_taker = { trigger_event = zz_valyria_magic.0026 } } #Isle of the Gods
					fallback = {
						task_contract_taker = {
							send_interface_toast = {
								title = poi_magic.visit
								left_icon = task_contract_taker
								custom_tooltip = poi_valyria_magic.effect
								if = { 
									limit = { 
										NOT = {
											has_trait = zz_magister
										}
									}
									trigger_event =  zz_valyria_magic.9000
									
								}
								else = {
									random = {
										chance = 75
										trigger_event =  zz_valyria_magic.9000
									}
								}
							}
						}
					}
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}

zz_valyria_magic_study_magic_mystery = {
	group = zz_valyria_magic_contracts_learn_mystery
	icon = "gfx/interface/icons/activity_types/activity_zz_magic_education_icon.dds"

	travel = yes
	use_diplomatic_range = yes

	weight = {
		# Standard weights.
		value = task_contract_weight_default_value
		add = laamp_contracts_weight_up_learning_value
		# Employer weights.
		scope:employer = {
			add = {
				# Weight up.
				## Traits that are likely to run into these kinds of political troubles but not super able to deal with them.
				if = {
					limit = { has_trait = honest }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = impatient }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = arbitrary }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = fickle }
					add = task_contract_weight_bonus_employer_has_associated_traits_value
				}
				# Weight down.
				## Traits that would avoid or swiftly resolve the issue.
				if = {
					limit = { has_trait = deceitful }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = patient }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = just }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				if = {
					limit = { has_trait = stubborn }
					add = task_contract_weight_malus_employer_has_associated_traits_value
				}
				# Restrictions.
				min = task_contract_weight_employer_associated_traits_min_value
				max = task_contract_weight_employer_associated_traits_max_value
			}
			# Don't weight down barons like we usually would, because we _only_ want barons here.
			# This weight is an override, so make sure it's placed at the bottom.
			add = task_contract_weight_employer_contact_list_value
		}
	}

	# Validity Triggers
	valid_to_create = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer.highest_held_title_tier = tier_county
	}
	valid_to_accept = {
		# Standard triggers.
		or = {
			has_trait = zz_magister
			has_character_flag = zz_valyria_magic_search
		}
		scope:employer = { is_landed = yes }
	}
	valid_to_continue = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}
	valid_to_keep = {
		# Standard triggers.
		task_contract_taker = { 
			or = {
				has_trait = zz_magister
				has_character_flag = zz_valyria_magic_search
			}
		} 
	}

	# On_actions
	on_accepted = {
		task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_accept_contract" }
		# For the intro event.
		save_scope_as = task_contract
		task_contract_employer = { save_scope_as = employer }
		# Then we roll our first event..
		task_contract_taker ?= { trigger_event = zz_valyria_magic_contracts.00010 }
	}
	on_create = {
		scope:contract = {
			# Make our gold rewards static.
		}
	}

	# Rewards
	task_contract_reward = {
		success_standard = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					zz_always_random_gain_magic_scroll = yes
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		success_critical = {
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_success_on_contract" }
				task_contract_taker = {
					zz_always_random_gain_magic_scroll = yes
				}
				task_contract_taker = { task_successful_noncrim_contract_count_effect = yes }
			}
		}
		failure_standard = {
			positive = no
			should_print_on_complete = yes
			effect = {
				task_contract_taker = { play_sound_effect = "event:/DLC/EP3/SFX/UI/Contracts/ep3_ui_contracts_failure_on_contract" }
			}
		}
	}
}
