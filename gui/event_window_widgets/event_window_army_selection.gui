widget = {
	allow_outside = yes
	name = "event_window_army_selection"
	size = { 350 250 }
	
	vbox = {
		#text_single = {block "header" { text = "This is a Header" } }
		container = {
			allow_outside = yes
			layoutpolicy_horizontal = expanding
			layoutpolicy_vertical = expanding
			datacontext = "[GetPlayer.MakeScope]"
			scrollbox = {
				layoutpolicy_horizontal = expanding
				layoutpolicy_vertical = expanding
				minimumsize = { 350 250 }
				blockoverride "scrollbox_content" {
					vbox = {
						datamodel = "[Scope.GetList('possible_armies')]"
						
						item = {
							button_standard = {
								size = { 300 45 }
								datacontext = "[Scope.Army]"
								
								down = "[GetScriptedGui( 'zz_valyria_army_selecting' ).IsShown( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_army', Army.MakeScope ).End )]"
								onclick = "[GetScriptedGui( 'zz_valyria_army_selecting' ).Execute( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_army', Army.MakeScope ).End )]"
								
								text_single = {
									text = "[Army.GetName]"
									max_width = 280
									parentanchor = center
									align = center|nobaseline
									#default_format = "#high"
									using = Font_Size_Medium
								}
							}
						}
					}
					spacer = { size = { 0 20 } }
				}
			}
		}
	}
}