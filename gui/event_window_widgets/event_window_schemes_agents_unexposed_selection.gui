widget = {
	allow_outside = yes
	name = "event_window_schemes_agents_unexposed_selection"
	size = { 350 250 } # Size of the insert inside the event window; needs to fit under the text box since a margin will only push the content and not the widget
	
	vbox = {
		#text_single = {block "header" { text = "This is a Header" } }
		container = {
			allow_outside = yes
			layoutpolicy_horizontal = expanding
			layoutpolicy_vertical = expanding
			datacontext = "[GetPlayer.MakeScope]"
			scrollbox = {
				layoutpolicy_horizontal = expanding
				layoutpolicy_vertical = expanding
				minimumsize = { 350 250 }
				blockoverride "scrollbox_content" {
					vbox = {
						datamodel = "[Scope.GetList('possible_schemes')]"
						
						item = {
							button_standard = {
								size = { 300 45 }
								datacontext = "[Scope.Scheme]"
								
								down = "[GetScriptedGui( 'zz_valyria_scheme_agents_unexposed_selecting' ).IsShown( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_scheme', Scheme.MakeScope ).End )]"
								onclick = "[GetScriptedGui( 'zz_valyria_scheme_agents_unexposed_selecting' ).Execute( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_scheme', Scheme.MakeScope ).End )]"
								
								widget = {
									size = { 90 45 }
									parentanchor = left
								}
								widget = {
									size = { 260 45 }
									parentanchor = right
									text_single = {
										text = "[Scheme.GetName]"
										parentanchor = center
										#default_format = "#high"
										using = Font_Size_Medium
									}
								}
							}
						}
					}
					spacer = { size = { 0 20 } }
				}
			}
		}
	}
}