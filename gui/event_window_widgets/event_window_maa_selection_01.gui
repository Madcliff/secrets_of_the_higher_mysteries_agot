widget = {
	allow_outside = yes
	name = "event_window_maa_selection_01"
	size = { 350 250 } # Size of the insert inside the event window; needs to fit under the text box since a margin will only push the content and not the widget
	
	vbox = {
		#text_single = {block "header" { text = "This is a Header" } }
		container = {
			allow_outside = yes
			layoutpolicy_horizontal = expanding
			layoutpolicy_vertical = expanding
			datacontext = "[GetPlayer.MakeScope]"
			scrollbox = {
				layoutpolicy_horizontal = expanding
				layoutpolicy_vertical = expanding
				minimumsize = { 350 250 }
				blockoverride "scrollbox_content" {
					vbox = {
						datamodel = "[Scope.GetList('possible_regiments_01')]"
						
						item = {
							button_standard = {
								size = { 300 45 }
								datacontext = "[Scope.Regiment]"
								
								down = "[GetScriptedGui( 'zz_valyria_regiment_selecting_01' ).IsShown( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_regiment_01', Regiment.MakeScope ).End )]"
								onclick = "[GetScriptedGui( 'zz_valyria_regiment_selecting_01' ).Execute( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_regiment_01', Regiment.MakeScope ).End )]"

								text_single = {
									text = "[Regiment.GetName]"
									max_width = 280
									parentanchor = center
									align = center|nobaseline
									#default_format = "#high"
									using = Font_Size_Medium
								}
								text_single = {
									text = "[Regiment.BuildCurrentRegimentCountString|V]"
									default_format = "#high"
								}
							}
						}
					}
					spacer = { size = { 0 20 } }
				}
			}
		}
	}
}