widget = {
	allow_outside = yes
	name = "event_window_culture_tradition_selection"
	size = { 350 250 } # Size of the insert inside the event window; needs to fit under the text box since a margin will only push the content and not the widget
	
	vbox = {
		#text_single = {block "header" { text = "This is a Header" } }
		container = {
			allow_outside = yes
			layoutpolicy_horizontal = expanding
			layoutpolicy_vertical = expanding
			datacontext = "[GetPlayer.MakeScope]"
			scrollbox = {
				layoutpolicy_horizontal = expanding
				layoutpolicy_vertical = expanding
				minimumsize = { 350 250 }
				blockoverride "scrollbox_content" {
					vbox = {
						datamodel = "[Scope.GetList('possible_culture_tradition')]"
						
						item = {
							button_standard = {
								size = { 300 45 }
								datacontext = "[Scope.CultureTradition]"
								
								down = "[GetScriptedGui( 'zz_valyria_culture_tradition_selecting' ).IsShown( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_culture_tradition', CultureTradition.MakeScope ).End )]"
								onclick = "[GetScriptedGui( 'zz_valyria_culture_tradition_selecting' ).Execute( GuiScope.SetRoot( GetPlayer.MakeScope ).AddScope( 'selected_culture_tradition', CultureTradition.MakeScope ).End )]"
								tooltip = "[CultureTradition.GetName]"

								text_single = {
									text = "[CultureTradition.GetNameNoTooltip]"
									max_width = 280
									parentanchor = center
									align = center|nobaseline
									#default_format = "#high"
									using = Font_Size_Medium
								}
							}
						}
					}
					spacer = { size = { 0 20 } }
				}
			}
		}
	}
}